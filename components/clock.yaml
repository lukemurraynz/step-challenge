apiVersion: dapr.io/v1alpha1
kind: Component
metadata:
  name: clock-cron
spec:
  type: bindings.cron
  version: v1
  metadata:
    - name: schedule
      value: "@every 60s"
    - name: direction
      value: "input"
scopes:
  - clock